name: dpp
description: A better dart pub publish, by running tests, fixes, lint, update CHANGELOG file and git commands.
version: 1.1.0
repository: https://github.com/insign/dart_dpp

environment:
  sdk: '>=2.19.2 <3.1.0'

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
dependencies:
  all_exit_codes: ^2.1.0
  args: ^2.4.0
  path: ^1.8.3
  yaml: ^3.1.1
  yaml2dart: ^1.0.0
